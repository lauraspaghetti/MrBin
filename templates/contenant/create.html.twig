{% extends "layout.html.twig" %}

{% block content %}

    <a href='{{ path("mrBin_contenant_showall") }}'>Voir les types de contenant déjà existants</a>

    <h2>Créer une fiche contenant.</h2>
    {{form_start(form, {'attr': {'novalidate' : 'novalidate'}} )}}
        {{ form_widget(form) }}
    {{ form_end(form) }}
    

{% endblock %}